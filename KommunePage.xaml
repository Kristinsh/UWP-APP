<!--VIS POSTNUMMER-->
    <Page
    xmlns:ViewModels="using:ViewModels"
    x:Class="UAR.KommunePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UAR"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <StackPanel>
        <StackPanel>
            <Button VerticalAlignment="Top" HorizontalAlignment="Left" Style="{StaticResource NavigationBackButtonNormalStyle}" Click="GoBack"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,60,0" HorizontalAlignment="Center">
            <TextBlock Text="Antall PostNummer:  " FontFamily="Georgia" FontSize="26" Margin="5"/>
            <TextBlock Text="{x:Bind VM.AntallPostNr}" FontFamily="Georiga" FontSize="26" Margin="5" TextAlignment="Center" FontWeight="Bold"></TextBlock>
        </StackPanel>
    </StackPanel> 
</Page>
